<!DOCTYPE html>
<html>
    <head>
        <title>블로그 메인</title>
    </head>
    <body>
        <h1>블로그 메인</h1>
        
        <% if (data.length>0) { %>
            <ul class="list-unstyled">
              <% for(var i=0; i < data.length; i++) { %>
              <li>
                  <a href="<%= data[i].id %>"><%= data[i].title %></a> - <%= data[i].author %>
              </li>
              <% } %>
            </ul>
        <% } %>
        
        <div>
            <% if(locals.user) { %>
                로그인 이메일: <%= locals.user.email %>
                <a href="/add">글쓰기</a>
                <a href="/account/logout">로그아웃</a>
            <% } else {%>
                <a href="/account/signup">회원가입</a>
                <a href="/account/login">로그인</a>
            <% } %>
        </div>
    </body>
</html>